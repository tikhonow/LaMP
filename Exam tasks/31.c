#include <stdio.h>
#include <stdlib.h>

int main()
{
    int n;
    scanf("%d", &n);
    int a[n];
    int s = 0;
    for (int i = 0; i < n; i++){
        a[i] = 0;
        s += a[i];
    }
    printf("%.3f" (s/n.000));
    return 0;
}
